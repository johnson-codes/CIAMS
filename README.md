# CIAMS

Local prototype workspace for the CIAMS site revamp.

## Remote

```text
https://github.com/johnson-codes/CIAMS.git
```

The GitHub repository is currently **empty** (no commits on the default branch). After you add a stack (e.g. Vite + React, Next.js, or plain HTML/CSS), commit and push from your machine’s Terminal:

```bash
cd /Users/johnson/Me/CIAMS-prototype
git remote add origin https://github.com/johnson-codes/CIAMS.git   # if not already added
git add -A && git commit -m "Initial prototype" && git push -u origin main
```

## Open in Cursor

Use **File → Open Folder** and choose `/Users/johnson/Me/CIAMS-prototype`, or ask the agent to **move the workspace root** to this path so edits and git run in one place.
